# bot/framework/signals/crypto/
"""CRYPTO / BLOCKCHAIN DESK — placeholder, ready for signals.

When the crypto venue (e.g. ccxt) is wired, drop `Signal` subclasses here and
`@register(...)` them. Continuous 24/7 price; no special handling needed by the
core. Nothing is registered yet.
"""
