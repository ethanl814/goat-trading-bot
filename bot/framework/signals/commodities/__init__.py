# bot/framework/signals/commodities/
"""COMMODITIES / FUTURES DESK — placeholder, ready for signals.

When a futures venue is wired, drop `Signal` subclasses here. Note futures
expire and roll (handled by the venue adapter + Resolution path). Nothing is
registered yet.
"""
